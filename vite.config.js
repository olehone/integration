import { defineConfig } from 'vite';
import vue from 'vite-plugin-vue2';

// Export the configuration for Vite
export default defineConfig({
//   plugins: [vue()]//, // Use vite-plugin-vue2 for Vue 2 support
//   resolve: {
//     alias: {
//       '@': '/src', // Assuming the source files are in the /src directory
//     },
//   },
//   test: {
//     globals: true,  // Use globals for testing (e.g., `describe`, `it`, `expect` available globally)
//     environment: 'jsdom', // Use jsdom to simulate a browser environment
//     include: ['**/*.{test,spec}.?(c|m)[jt]s?(x)'], // Look for test files with appropriate extensions
//     transformMode: {
//       web: [/\.vue$/],  // Make sure .vue files are handled by Vite
//     },
//   },
});
